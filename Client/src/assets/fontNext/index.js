import { Archivo, Concert_One } from "next/font/google";

export const ConcertOne = Concert_One({
  subsets: ["latin"],
  weight: "400",
  style: ["normal"],
});

export const achirvo = Archivo({
  subsets: ["vietnamese"],
  weight: "400",
  style: ["normal"],
});
